'use client';

// Re-export the edit contract page component for the /contracts/[id]/edit route
// This allows the route /contracts/[id]/edit to work alongside /edit-contract/[id]
export { default } from '../../edit-contract/[id]/page';

