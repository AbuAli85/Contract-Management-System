name: Database Migration

on:
  push:
    branches: [main]
    paths:
      - 'supabase/migrations/**'
      - 'supabase/functions/**'
      - '.github/workflows/migrate.yml'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: true
        default: 'production'
        type: choice
        options:
          - staging
          - production

jobs:
  validate-migrations:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Validate migration naming and ordering
        run: npm run supabase:validate-migrations

  deploy-migrations:
    runs-on: ubuntu-latest
    needs: validate-migrations
    environment: ${{ github.event.inputs.environment || 'production' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Set project reference
        id: set-project-ref
        run: |
          ENV="${{ github.event.inputs.environment || 'production' }}"
          if [ "$ENV" = "production" ]; then
            echo "ref=${{ secrets.SUPABASE_PROJECT_REF }}" >> $GITHUB_OUTPUT
          else
            echo "ref=${{ secrets.SUPABASE_STAGING_PROJECT_REF || secrets.SUPABASE_PROJECT_REF }}" >> $GITHUB_OUTPUT
          fi

      - name: Deploy to Supabase
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_REF: ${{ steps.set-project-ref.outputs.ref }}
        run: |
          echo "Deploying to ${{ github.event.inputs.environment || 'production' }}"

          # Link to Supabase project
          echo "🔗 Linking to Supabase project..."
          supabase link --project-ref $SUPABASE_PROJECT_REF --password ${{ secrets.SUPABASE_DB_PASSWORD || '' }}

          # Deploy database migrations
          echo "🔄 Deploying database migrations..."
          supabase db push --linked

          # Deploy Edge Functions (if any exist)
          if [ -d "supabase/functions" ] && [ "$(ls -A supabase/functions)" ]; then
            echo "🔄 Deploying Edge Functions..."
            supabase functions deploy --linked
          else
            echo "ℹ️  No Edge Functions to deploy"
          fi

          echo "✅ Deployment completed successfully"

      - name: Verify deployment
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_REF: ${{ steps.set-project-ref.outputs.ref }}
        run: |
          echo "🔍 Verifying deployment..."

          # Link to Supabase project (if not already linked)
          supabase link --project-ref $SUPABASE_PROJECT_REF --password ${{ secrets.SUPABASE_DB_PASSWORD || '' }} || true

          # Check if migrations were applied
          echo "📊 Checking migration status..."
          supabase db diff --linked || echo "⚠️  Could not check diff (this is okay if no changes)"

          # List deployed functions (if any exist)
          if [ -d "supabase/functions" ] && [ "$(ls -A supabase/functions)" ]; then
            echo "📋 Listing deployed functions..."
            supabase functions list --linked || echo "⚠️  Could not list functions"
          fi

          echo "✅ Deployment verification completed"

      - name: Run post-deployment tests
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_REF: ${{ steps.set-project-ref.outputs.ref }}
        run: |
          echo "🧪 Running post-deployment tests..."

          # Link to Supabase project (if not already linked)
          supabase link --project-ref $SUPABASE_PROJECT_REF --password ${{ secrets.SUPABASE_DB_PASSWORD || '' }} || true

          # Test database connectivity (dry run only - don't reset production!)
          if [ "${{ github.event.inputs.environment || 'production' }}" != "production" ]; then
            echo "🔍 Testing database connectivity..."
            supabase db diff --linked || echo "⚠️  Database connectivity check skipped"
          else
            echo "ℹ️  Skipping database reset in production (safety)"
          fi

          # Test Edge Functions
          echo "Testing Edge Functions..."
          # Add your function tests here

          echo "✅ Post-deployment tests completed"

  notify-deployment:
    runs-on: ubuntu-latest
    needs: deploy-migrations
    if: always()
    steps:
      - name: Notify deployment status
        run: |
          if [ "${{ needs.deploy-migrations.result }}" = "success" ]; then
            echo "✅ Database migration deployment successful"
            # Add notification logic here (Slack, email, etc.)
          else
            echo "❌ Database migration deployment failed"
            # Add failure notification logic here
          fi
